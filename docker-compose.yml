services:
  postgres:
    image: postgres:18
    container_name: learning-management-system
    restart: always

    environment:
      POSTGRES_DB: learning-management-system
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres

    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data

volumes:
  postgres_data: